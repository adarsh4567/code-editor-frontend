const ACTIONS = {    // define the actions
    JOIN:'join',
    JOINED:'joined',
    DISCONNECTED:'disconnected',
    SYNC_CODE:'sync-code',
    CODE_CHANGE:'code-change',
    LEAVE:'leave'
}

module.exports = ACTIONS;